{% extends 'SiteMainBundle:Frontend:layout.html.twig' %}

{% block content %}
    <div class="payment-form">
        <form action=""></form>
        <div class="form">
            <h1>Оплата</h1>
            {{ form_start(form, { 'style': 'horizontal', attr: {novalidate: 'novalidate'} }) }}
            {{ form_row(form.tariff) }}
            {{ form_row(form.numberMonth) }}
            {{ form_widget(form.submit) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}